<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.parkhere">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".main.HomeActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".main.RegisterActivity" />
        <activity android:name=".main.ValidationActivity" />
        <activity android:name=".main.CreateProviderProfileActivity" />
        <activity android:name=".provider.ProviderHomeActivity" />
        <activity android:name=".main.CreateSeekerProfileActivity" />
        <activity android:name=".seeker.SeekerHomeActivity" />
        <activity android:name=".seeker.list_view_screen" />
        <activity android:name=".seeker.filter_window" />
        <activity android:name=".seeker.invoice_screen" />
        <activity android:name=".seeker.MapsActivity" />
        <activity android:name=".seeker.parking_space_screen" />
        <activity android:name=".seeker.ratings_screen"></activity>
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
    </application>

</manifest>